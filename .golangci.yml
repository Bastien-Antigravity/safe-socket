version: "2"
run:
  timeout: 5m

linters:
  enable:
    - staticcheck
    - govet
    - ineffassign
    - typecheck

issues:
  exclude-rules:
    - linters:
        - staticcheck
      text: "QF1008"
